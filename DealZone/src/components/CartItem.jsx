const CartItem = () => {
    return (
        <div>
            <h2>Item Name</h2>
            <button>Delete</button>
            <button>Move to Wishlist</button>
        </div>
    );
};

export default CartItem;
